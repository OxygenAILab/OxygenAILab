# Oxygen AI website

React + Vite implementation of the Oxygen AI landing page.

## Commands

```bash
npm install
npm run dev
npm run build
npm run preview
```

Production links are centralized in `src/config.ts`. The deployment branch uses the GitHub Actions workflow in `.github/workflows/deploy.yml`.
